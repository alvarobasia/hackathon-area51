import styled from 'styled-components';

export const BotIconButton = styled.div`
  position: fixed;
  bottom: 32px;
  left: 32px;

  button {
    background-color: #55b3f3;
    border: none;
    padding: 12px;
    border-radius: 100%;
  }
`;
